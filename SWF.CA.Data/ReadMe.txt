Diese Klasse kapselt den DataLayer der Applikation.

Für den DB Zugriff wird Entity Framework Core verwendet. Für das Testprojekt wird einen neue DB am LocalDb Server von Visual Studio angelegt.

Es gibt ein Entity (InuranceOption) und den CaContext der ein Repoistory für den DB Zugriff ist.

Beim Anlegend er DB werden automatisch 3 Einträge geseedet.